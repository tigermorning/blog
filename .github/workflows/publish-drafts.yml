name: Publish scheduled drafts

on:
  schedule:
    - cron: '0 0 * * *'   # 한국시간(KST) 09:00 — 대기열에서 1개 발행
    - cron: '0 4 * * *'   # 한국시간(KST) 13:00 — 대기열에서 1개 발행
    - cron: '0 8 * * *'   # 한국시간(KST) 17:00 — 대기열에서 1개 발행
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Publish next queued draft
        run: bash .github/scripts/publish-drafts.sh
