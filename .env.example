# 비공개 독서 노트(잠긴 장 페이지)를 다시 암호화·복호화할 때 쓰는 비밀번호.
#
#   1) 이 파일을 .env 로 복사한다.  cp .env.example .env
#   2) 아래 값에 실제 비밀번호를 적는다.
#
# .env 는 .gitignore 에 들어 있어 저장소에 올라가지 않는다. 다만 평문이므로
# 이 PC에만 남는다 — 원본은 비밀번호 관리자에도 따로 보관할 것.
# 값에 공백이나 # 이 들어가면 따옴표로 감싼다: BOOK_PASSWORD="my pass#1"
BOOK_PASSWORD=
